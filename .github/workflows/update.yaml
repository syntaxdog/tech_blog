# .github/workflows/update.yml 파일 내용

name: Tistory Auto Sync and Commit

on:
  # 1. 스케줄 설정: UTC 기준으로 매일 자정에 실행 (한국 시간 기준 오전 9시)
  schedule:
    - cron: '0 0 * * *'
  # 2. 수동 실행 옵션: GitHub 웹사이트에서 'Run workflow' 버튼을 눌러 즉시 실행할 수 있게 함
  workflow_dispatch:

jobs:
  sync:
    # 이 작업은 최신 Ubuntu 환경에서 실행됩니다.
    runs-on: ubuntu-latest

    steps:
      - name: 🔍 저장소 체크아웃 (코드 가져오기)
        # GitHub Actions에서 저장소 코드를 러너로 가져오는 표준 작업
        uses: actions/checkout@v4
        
      - name: 🐍 Python 환경 설정
        # Python 3.x 버전을 설정합니다.
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: 📦 Python 라이브러리 설치
        # requirements.txt 파일에 명시된 모든 라이브러리를 설치합니다.
        run: pip install -r requirements.txt

      - name: ⚙️ Git 사용자 정보 설정
        # 스크립트가 커밋을 만들 때 사용할 이름과 이메일을 설정합니다.
        run: |
          git config user.name github-actions
          git config user.email github-actions@users.noreply.github.com
          
      - name: 🚀 스크립트 실행 및 커밋/푸시
        # 파이썬 스크립트를 실행하여 티스토리 글을 동기화합니다.
        # || echo "..." 는 변경 사항이 없을 경우 스크립트가 종료되더라도 에러로 처리하지 않게 합니다.
        run: |
          python update_posts.py
          git commit -a -m "Tistory post updated by action (Auto-sync)" || echo "No changes to commit"
          git push
